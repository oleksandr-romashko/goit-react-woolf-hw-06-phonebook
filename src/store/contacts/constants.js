/**
 * Constants related to contacts.
 */
const CONTACTS = {
  INITIAL_STATE: [],
  TYPES: Object.freeze({
    ADD_CONTACT: "contacts/addContact",
    DELETE_CONTACT_BY_ID: "contacts/deleteContactById",
  }),
};

export default CONTACTS;